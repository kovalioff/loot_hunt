services:
  bot:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /data/loot_hunt.db
    volumes:
      - loot-hunt-data:/data
    restart: unless-stopped
    stop_grace_period: 20s

volumes:
  loot-hunt-data:
